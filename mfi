#!/usr/bin/env bash
# mfi — Medicaid Inspector command-line wrapper (POSIX shells).
# Calls the CLI as a module so relative imports inside backend/ resolve.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
exec python3 -m backend.cli.mfi "$@"
